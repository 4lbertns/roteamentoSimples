function sum(x, y) {
    return `A soma entre ${x} e ${y} é igual a: ${x + y}.`;
};

function sub(x, y) {
    return `A substração entre ${x} e ${y} é igual a: ${x - y}.`;
};

module.exports = {
    sum,
    sub
};